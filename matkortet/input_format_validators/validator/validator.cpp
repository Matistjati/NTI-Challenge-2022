#include "validator.h"

#include <bits/stdc++.h>
using namespace std;

void ReadLn(int len) {

	for (int j = 0; j < len; j++) {
		char ch = Char();
	}
	Endl();
}

void run()
{
    for (int i = 0; i < 4; i++) 
    {
        char c = Char();
        assert((c>='0'&&c<='9')||c=='.');
    }
    
    Endl();
	Eof();
}
