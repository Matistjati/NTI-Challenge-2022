#include "validator.h"

#include <bits/stdc++.h>
using namespace std;


void run()
{
	int s = Int(1, Arg("max_s")); Space();
	int b = Int(1, Arg("max_b")); Space();
    int k = Int(1, Arg("max_k")); Endl();

	Eof();
}
