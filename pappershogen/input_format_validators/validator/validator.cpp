#include "validator.h"

#include <bits/stdc++.h>
using namespace std;

void ReadLn(int len) {

	for (int j = 0; j < len; j++) {
		
	}
	Endl();
}

void run()
{
	int n = Int(1, Arg("max_n")); Endl();

    for (int i = 0; i < n; i++)
    {
        char ch = Char();
        assert(ch=='U'||ch=='N');
    }
    Endl();
	Eof();
}
